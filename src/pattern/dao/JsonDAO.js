export default class JsonDAO{
    write(objects){
        console.log("-----------");
        objects.forEach(element => {
            console.log(element);
        });
        console.log("-----------");
    }
}
